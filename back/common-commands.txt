#### Create .venv folder
python -m venv .venv

#### Install/Uninstall dep with poetry
poetry add dep
poetry remove dep
poetry install

#### Migration
flask db init
flask db migrate -m "Initial migration"
flask db upgrade