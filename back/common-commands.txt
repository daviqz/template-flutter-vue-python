#### Create .venv folder
python -m venv .venv

#### Generate and install versions
pip freeze > requirements.txt
pip uninstall -r requirements.txt -y
pip install -r requirements.txt

#### Install/Uninstall dep
pip install dep
pip uninstall dep


#### Migration
flask db init
flask db migrate -m "Initial migration"
flask db upgrade